#!/bin/bash
sudo bash <(curl -s http://example.com/update.sh)
sudo bash <(curl -s http://example.com/install-trend.sh)
sudo bash <(curl -s http://example.com/cloud-init.sh)
sudo bash <(curl -s http://example.com/cleanup.sh)
